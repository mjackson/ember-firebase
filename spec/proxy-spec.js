var expect = chai.expect;

describe('Firebase.Proxy', function () {

  it('has the correct string representation', function () {
    expect(Firebase.Proxy + '').to.equal('Firebase.Proxy');
  });

});
